name: Pull Request Workflow

on:
  pull_request:
    branches:
      - main_abc
      - develop_abc

jobs:
  validate-template-code:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install AWS SAM CLI
        run: |
          pip install --user aws-sam-cli

      - name: Validate SAM Templates
        run: |
          echo "Validating SAM templates"
          echo "Current directory: $(pwd)"
          echo ls -lart
          find . -path "./.github" -prune -o -name "version.yaml" -prune -o \( -name "*.yaml" -o -name "*.yml" \) -print0 | while IFS= read -r -d '' template; do
           echo "Validating template: $template"
           echo "Current directory: $(pwd)"
           echo "Template directory: $(dirname "$template")"
           echo "Template name: $(basename "$template")"
           echo "Template path: $template"
           echo "Template file: $(cat "$template")"
           echo ls -lart
           sam validate --template-file "$template" --region us-east-2 --lint
          done        
        shell: bash

  security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run CFN Nag Security Checks
        uses: stelligent/cfn_nag@master
        with:
          input_path: .
          extra_args: -o sarif
          output_path: cfn_nag.sarif
      - name: Upload CFN Nag SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: cfn_nag.sarif    
          category: security